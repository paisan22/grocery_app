<div class="row">

    <div class="col-md-12">

        <div class="row">

            <div class="col-md-8">
                <div class="page-header">
                    <h1>Grocery App</h1>
                    <small>Paisan Rietbroek</small>
                </div>
            </div>



            <div class="col-md-4">
                <div class="jumbotron">

                    <div class="row">
                        <h4><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Hello {{ user }}</h4>
                    </div>

                    <div class="row">

                        <a href="{{ path('homepage') }}" class="btn btn-primary">
                            <span class="glyphicon glyphicon-list" aria-hidden="true"></span> Items</a>

                        <a href="{{ path('category_page') }}" class="btn btn-primary">
                            <span class="glyphicon glyphicon-filter" aria-hidden="true"></span> Categories</a>

                        <a href="{{ path('logout') }}" class="btn btn-danger">
                            <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> Logout</a>
                    </div>

                </div>
            </div>
        </div>

    </div>

</div>